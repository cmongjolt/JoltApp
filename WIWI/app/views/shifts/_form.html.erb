<%= form_with(model: shift, local: true) do |form| %>
  <% if shift.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shift.errors.count, "error") %> prohibited this shift from being saved:</h2>

      <ul>
      <% shift.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :userId %>
    <%= form.text_field :userId, id: :shift_userId %>
  </div>

  <div class="field">
    <%= form.label :positionId %>
    <%= form.text_field :positionId, id: :shift_positionId %>
  </div>

  <div class="field">
    <%= form.label :startTime %>
    <%= form.text_field :startTime, id: :shift_startTime %>
  </div>

  <div class="field">
    <%= form.label :endTime %>
    <%= form.text_field :endTime, id: :shift_endTime %>
  </div>

  <div class="field">
    <%= form.label :notes %>
    <%= form.text_field :notes, id: :shift_notes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
